# VYRAL HQ

The growth operating system for photographers.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

The site uses relative production asset paths, so the contents of `dist/` can also be uploaded to another static host such as GoDaddy.
